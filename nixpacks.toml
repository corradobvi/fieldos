[phases.install]
cmds = ["corepack enable && corepack prepare pnpm@10.33.2 --activate && pnpm install"]

[phases.build]
cmds = ["pnpm --filter @workspace/fieldos run build"]

[start]
cmd = "pnpm --filter @workspace/fieldos run serve"
